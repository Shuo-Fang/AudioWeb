<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.audioweb.work.mapper.WorkSchemeMapper">
    
    <resultMap type="WorkScheme" id="WorkSchemeResult">
        <result property="schemeId"    column="scheme_id"    />
        <result property="schemeName"    column="scheme_name"    />
        <result property="status"    column="status"    />
        <result property="remark"    column="remark"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="delFlag"    column="del_flag"    />
        <result property="schemePriority"    column="scheme_priority"    />
    </resultMap>

    <sql id="selectWorkSchemeVo">
        select scheme_id, scheme_name, status, remark, create_by, create_time, update_by, update_time, del_flag, scheme_priority from work_scheme
    </sql>

    <select id="selectWorkSchemeList" parameterType="WorkScheme" resultMap="WorkSchemeResult">
        <include refid="selectWorkSchemeVo"/>
        <where>  
            <if test="schemeName != null  and schemeName != ''"> and scheme_name like concat('%', #{schemeName}, '%')</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
            <if test="schemePriority != null  and schemePriority != ''"> and scheme_priority = #{schemePriority}</if>
        </where>
    </select>
    
    <select id="selectWorkSchemeById" parameterType="Long" resultMap="WorkSchemeResult">
        <include refid="selectWorkSchemeVo"/>
        where scheme_id = #{schemeId}
    </select>
        
    <insert id="insertWorkScheme" parameterType="WorkScheme" useGeneratedKeys="true" keyProperty="schemeId">
        insert into work_scheme
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="schemeName != null  and schemeName != ''">scheme_name,</if>
            <if test="status != null  and status != ''">status,</if>
            <if test="remark != null  and remark != ''">remark,</if>
            <if test="createBy != null  and createBy != ''">create_by,</if>
            <if test="createTime != null ">create_time,</if>
            <if test="updateBy != null  and updateBy != ''">update_by,</if>
            <if test="updateTime != null ">update_time,</if>
            <if test="delFlag != null  and delFlag != ''">del_flag,</if>
            <if test="schemePriority != null  and schemePriority != ''">scheme_priority,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="schemeName != null  and schemeName != ''">#{schemeName},</if>
            <if test="status != null  and status != ''">#{status},</if>
            <if test="remark != null  and remark != ''">#{remark},</if>
            <if test="createBy != null  and createBy != ''">#{createBy},</if>
            <if test="createTime != null ">#{createTime},</if>
            <if test="updateBy != null  and updateBy != ''">#{updateBy},</if>
            <if test="updateTime != null ">#{updateTime},</if>
            <if test="delFlag != null  and delFlag != ''">#{delFlag},</if>
            <if test="schemePriority != null  and schemePriority != ''">#{schemePriority},</if>
         </trim>
    </insert>

    <update id="updateWorkScheme" parameterType="WorkScheme">
        update work_scheme
        <trim prefix="SET" suffixOverrides=",">
            <if test="schemeName != null  and schemeName != ''">scheme_name = #{schemeName},</if>
            <if test="status != null  and status != ''">status = #{status},</if>
            <if test="remark != null  and remark != ''">remark = #{remark},</if>
            <if test="createBy != null  and createBy != ''">create_by = #{createBy},</if>
            <if test="createTime != null ">create_time = #{createTime},</if>
            <if test="updateBy != null  and updateBy != ''">update_by = #{updateBy},</if>
            <if test="updateTime != null ">update_time = #{updateTime},</if>
            <if test="delFlag != null  and delFlag != ''">del_flag = #{delFlag},</if>
            <if test="schemePriority != null  and schemePriority != ''">scheme_priority = #{schemePriority},</if>
        </trim>
        where scheme_id = #{schemeId}
    </update>

    <delete id="deleteWorkSchemeById" parameterType="Long">
        delete from work_scheme where scheme_id = #{schemeId}
    </delete>

    <delete id="deleteWorkSchemeByIds" parameterType="String">
        delete from work_scheme where scheme_id in 
        <foreach item="schemeId" collection="array" open="(" separator="," close=")">
            #{schemeId}
        </foreach>
    </delete>
    
</mapper>